extends layout

block hero-unit
  .jumbotron
    .container
      h1  여행은 살아보는 거야. </br> 전 세계의 독특한 숙소를 예약하고 현지인의 삶을 살아보세요.

block content
  .container
      form.form(action='/posts', method='POST')
        label(for='position') 위치
        input(type='postion', id='position', name='position' placeholder='position')
        label(for='date') 날짜
        input(type='checkin', id='checkin', name='checkin' placeholder='checkin')
        input(type='checkout', id='checkout', name='checkout' placeholder='checkout')
        label(for='people_number') 숙박인원
        input(type='checkin', id='checkin', name='checkin' placeholder='number of people')
        button.btn.btn-lg.btn-primary.btn-block(type='submit') 숙박검색 
        // 숙박검색을 누르면 
      
      // modal 형태로 회원 가입 창이 뜨게 함
      
      div.modal.fade(id='signup', role='dialog') 
        div.modal-dialog
          div.modal-content
            div.modal-header 
              button(type='button' ,class='close', data-dismiss='modal') x
            label 회원가입
            div.modal-body
              form(action='/users', method='POST') 
                .form-group
                  label(for='name') Name
                  input.form-control(type='text', id='name', name='name', placeholder='Name')
                .form-group
                  label(for='email') Email address
                  input.form-control(type='email', id='email', name='email', placeholder='Email')
                .form-group
                  label(for='password') Password
                  input.form-control(type='password', id='password', name='password', placeholder='Password')
                .form-group
                  label(for='password_confirmation') Password 확인
                  input.form-control(type='password', id='password_confirmation', name='password_confirmation', placeholder='Password')
                div.form-actions
                  a.btn.btn-default(href='javascript:window.history.back();') 뒤로 
                  | 
                  button.btn.btn-primary(type='submit') 회원가입 
           
      div.modal.fade(id='signin', role='dialog')
        div.modal-dialog
          div.modal-content
            div.modal-header
              button(type='button' ,class='close', data-dismiss='modal') x
            label 로그인
            div.modal-body
              form(action='/signin', method='POST')
                label(for='email', class='sr-only') Email address
                input(
                  type='email', id='email', name='email', class='form-control', 
                  placeholder='Email address', required, autofocus
                )
                label(for='password', class='sr-only') Password
                input(
                  type='password', id='password', name='password', class='form-control', 
                  placeholder='Password', required, autofocus
                )
                button.btn.btn-lg.btn-primary.btn-block(type='submit') 로그인
                | 
                a.btn.btn-info.btn-lg.btn-block(href='/auth/facebook')
                  i.fa.fa-facebook.fa-lg
                  | &nbsp; &nbsp;Signin with Facebook
                hr
           
         